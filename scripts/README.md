# Scripts

## Claude Code + Vertex AI

- **`export-vertex-env.sh`** – Exports Vertex env vars. Use when the Claude Code extension still shows the login page:

  ```bash
  source scripts/export-vertex-env.sh
  cursor .   # or: code .
  ```

  See [docs/CLAUDE_VERTEX_SETUP.md](../docs/CLAUDE_VERTEX_SETUP.md) for full setup and workarounds.
